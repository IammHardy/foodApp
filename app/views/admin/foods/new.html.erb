<div class="max-w-2xl mx-auto mt-8 bg-white p-6 rounded shadow">
  <h1 class="text-2xl font-bold text-red-600 mb-6">Add New Food</h1>

  <%= render 'form', food: @food %>

  <div class="mt-4">
    <%= link_to '← Back to Foods', admin_foods_path, class: "text-gray-600 hover:underline" %>
  </div>
</div>
